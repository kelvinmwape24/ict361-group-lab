<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is blocked by default from API 28 up, which is why the
  emulator could never reach http://10.0.2.2:3000.

  This allows it for the development host ONLY. Every other host, including
  production, stays HTTPS-only — the API contract requires it.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
