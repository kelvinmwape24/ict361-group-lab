<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Without this the app cannot reach the API at all. It was missing. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- No camera, location or Bluetooth permission is needed for this lab. -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ICT361Lab"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" android:exported="false" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="false"
            android:parentActivityName=".ui.auth.LoginActivity" />
        <activity android:name=".ui.student.StudentProfileActivity" android:exported="false" />
        <activity android:name=".ui.student.StudentEditActivity" android:exported="false"
            android:parentActivityName=".ui.student.StudentProfileActivity" />
        <activity android:name=".ui.lecturer.RosterActivity" android:exported="false" />
        <activity android:name=".ui.lecturer.StudentEditorActivity" android:exported="false"
            android:parentActivityName=".ui.lecturer.RosterActivity" />
        <activity android:name=".ui.sync.SyncStatusActivity" android:exported="false" />
    </application>
</manifest>
